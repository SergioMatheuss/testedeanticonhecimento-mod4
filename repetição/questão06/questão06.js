// Escrever um algoritmo que gera e escreve os números ímpares entre 100 e 200.

for (imp = 100; imp <=200; imp++
    )
    {
        if(imp%2===1){
            document.write(`${imp}\n`)
        }
    }
