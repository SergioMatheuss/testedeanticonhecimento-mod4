// Escreva um algoritmo que lê dois valores booleanos (lógicos) e então determina se ambos são 
// VERDADEIROS ou FALSOS.

let a = 1;
let b = 1;

if (a === 1 && b === 1) {
    document.write(`Ambos são verdadeiros.`);
    a === true;
    b === false;
    
} 
if (b === 0) {
    document.write(`Ambos são falsos.`)
};