// Faça um algoritmo que leia os valores A, B, C e imprima na tela se a soma de A + B é menor 
// que C.

let a = 10;
let b = 1; 
let c = 20;
soma = a + b;
if (soma < c ) {
    document.write(`A soma ${soma} é menor que c`);
};